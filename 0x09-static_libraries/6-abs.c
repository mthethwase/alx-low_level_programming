#include "main.h"
/**
 * _abc - program computes absolute value of an integer
 * @n: the integer to check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
